.well.sidebar-nav
  %ul.nav.nav-list
    - Section.all.each do |section|
      %li.nav-header= section.name
      - section.categories.each do |category|
        %li= link_to category.name, pages_path(category: category), method: :post, remote: true